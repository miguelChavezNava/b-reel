# B-Reel Mobile App

This project is a mobile app created to allow users to rank 10 movies through simple side-by-side comparisons, letting lists to be quickly made and shared easily with others.

The movie data was gathered from the <a href="https://www.omdbapi.com/">Open Movie Database<a> and then presented on a list with separate elements, showing the director, release year, and rating. Posters were curated and added onto the project through simple downloads, being loaded for each movie by their name. The ranking algorithm was made through implementing merge sort with these side-by-side comparisons creating and deleting branches, ranking the movies as the tree grows and shrinks until an order is determined.

This app was made with React for the user interface and coded with TypeScript, being publicly hosted through Expo Go. The ranking algorithm was scripted with JavaScript and connected to the React UI.

This project enables movie lovers to have a convenient place to rank their favorite movies and share them easily with others, making it simple to generate discussions of what the best films are, creating a better sense of community among people.

To run this project, download Expo Go from the app store, then scan this QR code to access the app:

<img width="256" height="256" alt="eas-update" src="https://github.com/user-attachments/assets/f58535ba-dd03-46a3-ab8c-e33543997ca3" />

We learned much about app design and how apps are created using React and TypeScript, as well as how to use API calls to retrieve data, lightening the data load on the app. We gained insight as how different app UI components work and general design principles to make the app have a clean visual style.

